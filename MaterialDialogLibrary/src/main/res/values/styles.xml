<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Material Design theme. -->
    <style name="Theme.MyApp" parent="Theme.MaterialComponents.Light">
        <item name="android:fontFamily">@font/montserrat</item>
    </style>

    <style name="Theme.MaterialDialog" parent="Theme.MaterialComponents.Dialog">
    </style>

    <!-- Theme for BottomSheetDialog -->
    <style name="BottomSheet" parent="@style/Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bottomsheet_back</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <style name="BaseBottomSheetDialog" parent="@style/Theme.Design.Light.BottomSheetDialog">
        <item name="android:windowIsFloating">false</item>
        <item name="bottomSheetStyle">@style/BottomSheet</item>
    </style>

    <style name="BottomSheetDialogTheme" parent="BaseBottomSheetDialog" />

    <!-- Theme for Dialog Title -->
    <style name="DialogTitle">
        <item name="android:fontFamily">@font/montserrat</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/colorTitle</item>
        <item name="android:layout_marginTop">12dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
    </style>

    <!-- Theme for Dialog Message -->
    <style name="DialogMessage">
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily">@font/montserrat</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:textColor">@color/colorMessage</item>
    </style>

    <!-- Theme for Dialog Positive Button -->
    <style name="DialogPositiveButton" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="android:layout_margin">4dp</item>
        <item name="android:fontFamily">@font/montserrat</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Theme for Dialog Negative Button -->
    <style name="DialogNegativeButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:layout_margin">4dp</item>
        <item name="android:fontFamily">@font/montserrat</item>
        <item name="android:textAllCaps">false</item>
    </style>
</resources>